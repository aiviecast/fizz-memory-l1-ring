[package]
name = "fizz_memory_l1_ring"
version = "0.1.0"
description = "Fizz memory L1 — per-user in-memory ring buffer of recent MemoryEvents (hot path; note/recall over NDJSON)"

[dependencies]
fizz_protocol = { git = "https://github.com/aiviecast/fizz-protocol", tag = "v0.2.2" }
